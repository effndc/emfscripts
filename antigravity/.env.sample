# EMF Manager Configuration
# Copy this file to .env and adjust values for your environment.

# -----------------------------------------------------------------------------
# Required Variables
# -----------------------------------------------------------------------------

# Base domain of the cluster (e.g. cluster.example.com).
# This is used to automatically derive Keycloak (keycloak.*) and EMF API (api.* OR emf.*) URLs.
CLUSTER_FQDN=cluster.example.com

# Password for the Keycloak Platform Admin.
KEYCLOAK_ADMIN_PASS=changeme

# -----------------------------------------------------------------------------
# Optional Variables (Defaults shown)
# -----------------------------------------------------------------------------

# Username for the Keycloak Platform Admin.
# KEYCLOAK_ADMIN_USER=admin

# Verify SSL Certificates. Set to false for self-signed certs (security risk).
# VERIFY_SSL=true

# Seconds to wait for resource provisioning (Org/Project/Group creation).
# POLL_TIMEOUT=300

# -----------------------------------------------------------------------------
# Proxy Configuration
# -----------------------------------------------------------------------------

# Usage: docker run --env-file .env ...
# Note: Docker will not automatically pick up proxy settings from host environment unless passed explicitly.

# Comma-separated domains to bypass proxy. Crucial for internal cluster communication.
# no_proxy=.cluster.local,.svc,127.0.0.1,localhost,keycloak.cluster.example.com

# HTTP Proxy URL
# http_proxy=http://proxy.example.com:8080

# HTTPS Proxy URL
# https_proxy=http://proxy.example.com:8080
